interface:
  display_name: "Task Manager"
  short_description: "Find and manage tasks, projects, and releases"
  default_prompt: "Use Task Manager to find my current work, help me select a task, and make only the changes I request."

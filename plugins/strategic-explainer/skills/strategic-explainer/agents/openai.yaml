interface:
  display_name: "Strategic Explainer"
  short_description: "Передать объяснение отдельному чистому субагенту"
  default_prompt: "Используй $strategic-explainer:strategic-explainer как router: создай новый default subagent на gpt-5.6-luna с reasoning_effort=max и fork_turns=none, затем передай exact role lock STRATEGIC_EXPLAINER_PROVIDER_V1, одну короткую user-facing задачу, exact scope и resolvable read-only anchors без inherited context или своего candidate."

policy:
  allow_implicit_invocation: true
